#include <iostream>
using namespace std;

/*
normalize (){
    Scale numeric values to 0-1
}
*/

void standardize(vector<vector<int>> &dataset)
{
    int columnNumber = dataset[0].size();

    for (int i = 0; i < columnNumber; i++)
    {
        for (int)
    }

    // Scale numeric values to zero mean and unit variance
}

void standardize(vector<vector<int>> &dataset)
{

    int testMean = 0;
    int testVariance = 0;
    int columnNumber = dataset[0].size();

    for (int i = 0; i < columnNumber; i++)
    {
        for (int j = 0; j < datas)
    }

    // Scale numeric values to zero mean and unit variance
}

/*
one_hot_encode(){
    Convert categorical column to binary dummy variables
}

lable_encode(){
    Convert categorical strings to integers
    Example: "red" → 0, "blue" → 1
}

train_test_split(){
    Split dataset into training and testing sets
}

shuffle_data(){
    Optional helper to shuffle rows before splitting
    Ensures random train/test samples
}



*/